# Playbook 관리자 가이드

## 목차

1. [로그인 / 로그아웃](#1-로그인--로그아웃)
2. [관리자 계정 관리](#2-관리자-계정-관리)
3. [학생 계정 관리](#3-학생-계정-관리)
4. [도서 관리](#4-도서-관리)
5. [대출 / 반납](#5-대출--반납)
6. [대출 이력](#6-대출-이력)
7. [통계](#7-통계)
8. [과정 관리](#8-과정-관리)
9. [약관 관리](#9-약관-관리)
10. [부록: 상태 코드 설명](#10-부록-상태-코드-설명)

---

## 1. 로그인 / 로그아웃

- 브라우저에서 서비스 주소로 접속 후 **관리자 ID / 비밀번호**를 입력해 로그인합니다.
- 동일 계정으로 중복 로그인 시 기존 세션이 자동 만료됩니다.
- 화면 우측 상단 프로필 영역에서 **로그아웃** 버튼을 눌러 세션을 종료합니다.
- 대출·반납 기능은 **PC 브라우저에서만** 사용 가능합니다. 모바일에서는 해당 기능이 제한됩니다.

---

## 2. 관리자 계정 관리

관리자 페이지 → **관리자 계정** 탭

### 관리자 권한 구분

| 구분 | 설명 |
|------|------|
| 전체 관리자 | 캠퍼스 제한 없이 모든 데이터 조회·관리 가능 |
| 캠퍼스 관리자 | 자신이 속한 캠퍼스의 데이터만 조회·관리 가능 |

### 기능 목록

**관리자 목록 조회**
- 전체 관리자 또는 캠퍼스 필터를 적용해 관리자 목록을 확인합니다.
- 표시 컬럼: ID, 이름, 캠퍼스, 디스코드 ID, 생성일

**새 관리자 추가**
- 우측 폼에서 ID, 이름, 캠퍼스, 비밀번호, 디스코드 ID를 입력하고 등록합니다.
- 캠퍼스를 지정하지 않으면 전체 관리자로 등록됩니다.

**관리자 정보 수정**
- 목록에서 수정할 관리자를 선택합니다.
- 현재 비밀번호 확인 후 디스코드 ID 또는 비밀번호를 변경할 수 있습니다.
- 전체 관리자는 다른 관리자 계정도 수정 가능합니다. 캠퍼스 관리자는 본인 계정만 수정 가능합니다.

**관리자 삭제**
- 삭제할 관리자 선택 → 삭제 버튼 → 비밀번호 확인 후 삭제합니다.
- 삭제된 계정은 복구할 수 없습니다.

**엑셀 내보내기**
- 현재 필터 조건에 맞는 관리자 목록을 `관리자계정.xlsx`로 다운로드합니다.

---

## 3. 학생 계정 관리

관리자 페이지 → **학생 계정** 탭

### 기능 목록

**학생 목록 조회**
- 이름·ID 검색, 상태 필터(정상 / 정지 / 연체), 캠퍼스 필터를 조합해 조회합니다.
- 표시 컬럼: 학생명, 학생 ID, 상태, 가입일, 과정명, 과정 기간
- 요약 통계: 총 학생 수 / 대출 가능 학생 수 / 연체 학생 수

**학생 상세 조회**
- 목록에서 학생을 클릭하면 상세 정보 패널이 열립니다.
- 기본 정보(이름, ID, 상태, 가입일)와 과정 정보(과정명, 기간, 진행 상태)를 확인할 수 있습니다.

**학생 계정 삭제**
- 상세 패널에서 삭제 버튼 클릭 → 확인 후 삭제합니다.
- 현재 대출 중인 도서가 있으면 삭제할 수 없습니다.

**엑셀 내보내기**
- 현재 필터 조건의 학생 목록을 `학생계정.xlsx`로 다운로드합니다.

---

## 4. 도서 관리

관리자 페이지 → **도서** 탭

### 도서 목록 조회

- **검색**: 제목, 저자, 출판사, ISBN으로 검색합니다.
- **필터**: 대분류, 중분류, 대출 상태(대출 중 / 대출 가능 / 대출 불가), 정렬 순서, 캠퍼스를 조합 적용합니다.
- 요약 통계: 표시된 도서 / 전체 도서 / 대출 중 / 대출 가능 / 대출 불가 / 출력 대기

### 도서 등록

우측 상단 **도서 등록** 버튼 → 도서 등록 페이지로 이동합니다.

1. **ISBN 검색**: ISBN을 입력하면 국립중앙도서관 API로 도서 정보(제목, 저자, 출판사, 출판일, 표지)를 자동으로 가져옵니다.
2. 자동 조회되지 않는 항목은 직접 입력합니다.
3. 대분류·중분류를 선택하고 바코드를 입력합니다.
4. 도서를 저장하면 목록에 즉시 반영됩니다.

### 도서 수정·삭제

- 목록에서 도서 행을 클릭하면 우측에 상세·수정 패널이 열립니다.
- 대분류, 중분류, 바코드를 수정할 수 있습니다.
- 삭제 버튼 클릭 → 확인 후 삭제합니다.

### 바코드 출력

- 목록 상단의 **바코드 출력 모드** 토글을 켭니다.
- 출력할 도서를 선택한 뒤 **바코드 인쇄** 버튼을 클릭합니다.
- 인쇄 시 시작 위치(라벨 용지의 몇 번째 칸부터 시작할지)를 설정할 수 있습니다.
- 인쇄가 완료된 도서는 **출력 완료** 처리되어 출력 대기 카운트에서 제외됩니다.

### 엑셀 내보내기

- 현재 필터 조건의 도서 목록을 `도서목록.xlsx`로 다운로드합니다.
- 대분류·중분류 한글명이 정확히 포함됩니다.

---

## 5. 대출 / 반납

관리자 페이지 상단 **대출/반납** 버튼 (또는 홈 화면 카드)

> PC 브라우저에서만 이용 가능합니다.

### 도서 대출

1. **도서 대출** 카드를 선택합니다.
2. 학생 바코드(또는 관리자 ID)와 도서 바코드를 스캔·입력합니다.
3. 대출 완료 시 디스코드로 알림이 전송됩니다.

**대출 제한 규칙**
- 학생은 최대 **2권**까지 동시 대출 가능합니다.
- 연체 중인 도서가 있으면 추가 대출이 불가합니다.
- 이미 대출 중인 도서는 중복 대출할 수 없습니다.

### 도서 반납

1. **도서 반납** 카드를 선택합니다.
2. 도서 바코드를 스캔·입력합니다.
3. 반납 완료 시 디스코드로 알림이 전송됩니다.
4. 해당 도서를 즐겨찾기한 학생에게도 반납 알림이 전송됩니다.

**연체 처리**
- 대출일로부터 **7일** 초과 시 연체 상태가 됩니다.
- 연체 반납 시 해당 계정은 **정지** 상태로 전환됩니다.

---

## 6. 대출 이력

관리자 페이지 → **대출 이력** 탭

### 기능 목록

**이력 조회 및 필터링**
- 기간(오늘 / 이번 주 / 이번 달 / 사용자 지정 / 전체), 상태(대출 중 / 반납 완료 / 연체), 캠퍼스, 도서명·사용자명 검색을 조합해 조회합니다.
- 요약 통계: 총 대출 건수 / 현재 대출 중 / 총 반납 건수 / 연체 도서

**이력 상세 조회**
- 목록에서 행을 클릭하면 도서 정보, 사용자 정보, 대출 정보(대출일, 반납예정일, 반납일)를 상세 패널에서 확인합니다.

**이력 삭제**
- 잘못 기록된 이력을 선택 후 삭제할 수 있습니다.

**엑셀 내보내기**
- 현재 필터 조건의 대출 이력을 `대출이력.xlsx`로 다운로드합니다.
- 컬럼: 도서명, 저자, ISBN, 바코드, 이름, 아이디, 과정, 대출일, 반납일, 상태

---

## 7. 통계

관리자 페이지 → **통계** 탭

### 조회 조건

- **캠퍼스**: 전체 또는 특정 캠퍼스 선택
- **과정**: 특정 과정 선택 시 해당 과정 학생의 통계만 표시

### 제공 통계

| 항목 | 설명 |
|------|------|
| 인기 도서 대분류 | 대출 횟수 기준 대분류 순위 (순위, 대분류, 대여 횟수) |
| 인기 도서 중분류 | 대출 횟수 기준 중분류 순위 (순위, 중분류, 대여 횟수) |
| 사용자 독서 랭킹 | 대출 횟수 기준 학생 순위 (순위, 사용자명, 대여 수) |

---

## 8. 과정 관리

관리자 페이지 → **과정 관리** 탭

내부에 **과정 관리**와 **캠퍼스 관리** 두 개의 하위 탭이 있습니다.

### 과정 관리

- 과정 목록을 조회합니다 (과정명, 캠퍼스, 시작일, 종료일).
- **과정 추가**: 과정명, 캠퍼스, 시작일, 종료일을 입력해 등록합니다.
- **과정 수정**: 목록에서 과정을 선택해 정보를 수정합니다.
- **과정 삭제**: 과정을 삭제합니다. 해당 과정에 소속된 학생이 있으면 삭제에 주의가 필요합니다.
- **엑셀 내보내기**: 현재 과정 목록을 `과정목록.xlsx`로 다운로드합니다.

### 캠퍼스 관리

- 캠퍼스 목록을 조회합니다.
- **캠퍼스 추가**: 캠퍼스명을 입력해 등록합니다.
- **캠퍼스 수정·삭제**: 목록에서 선택 후 처리합니다.

---

## 9. 약관 관리

관리자 페이지 → **약관 관리** 탭

- 회원가입 시 표시되는 약관 3종을 수정합니다.

| 탭 | 약관 종류 |
|----|----------|
| 1 | 이용약관 |
| 2 | 개인정보처리방침 |
| 3 | 디스코드 알림 동의서 |

- 내용은 **HTML 형식**으로 직접 편집합니다.
- 우측 **미리보기** 패널에서 실제 출력 모양을 실시간으로 확인할 수 있습니다.
- 수정 후 **저장** 버튼을 클릭합니다.

---

## 10. 부록: 상태 코드 설명

### 학생 / 관리자 계정 상태

| 상태 | 의미 |
|------|------|
| 정상 (available) | 대출 가능한 상태 |
| 정지 (stop) | 대출 불가 상태. 도서 2권 대출 중이거나 관리자가 정지 처리한 경우 |
| 연체 (overdue) | 반납 기한(7일)을 초과한 도서가 있는 상태 |

### 도서 대출 상태

| 상태 | 의미 |
|------|------|
| 대출 가능 | 현재 대출 중인 학생 없음 |
| 대출 중 | 현재 누군가에게 대출된 상태 |
| 대출 불가 | 분실·훼손 등으로 대출이 제한된 상태 |

### 연체 정책

- 대출일로부터 **7일** 초과 시 연체 처리됩니다.
- 연체 반납 후 계정은 **정지** 상태로 전환됩니다.
- 과정이 종료된 학생은 연체 반납 시 예외 처리됩니다 (정지 유지).
